const REGISTER = 'register'
const LOGIN = 'login'

export const paths = {
    register: REGISTER,
    login: LOGIN,
}

const pathTo = {
    register: `/${REGISTER}`,
    login: `/${LOGIN}`,
}

export { pathTo }
